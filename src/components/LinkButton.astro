---
export interface Props {
  href: string;
  className?: string;
  ariaLabel?: string;
  title?: string;
  disabled?: boolean;
  localePath?: string;
}

const {
  href,
  className,
  ariaLabel,
  title,
  disabled = false,
  localePath,
} = Astro.props;
---

{
  disabled ? (
    <span
      class={`group inline-block ${className}`}
      aria-label={ariaLabel}
      title={title}
      aria-disabled={disabled}
      data-nav-locale-path={localePath}
    >
      <slot />
    </span>
  ) : (
    <a
      {href}
      class={`group inline-block hover:text-skin-accent ${className}`}
      aria-label={ariaLabel}
      title={title}
      data-nav-locale-path={localePath}
    >
      <slot />
    </a>
  )
}
